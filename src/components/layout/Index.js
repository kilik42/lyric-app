import React from 'react';
import Tracks from '../tracks/Tracks';

const Index = (props) => {
  return (
    <React.Fragment>
        <Tracks />
    </React.Fragment>
  );
};



export default Index;
